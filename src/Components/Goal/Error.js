import React from 'react';

export default () => (
  <div id="error" className="error-invisible">A goal associated with this phone number already exists. Please delete it before creating a new one.</div>
)
